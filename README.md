# trader1

## 1. Project Overview

`trader1` is a Clojure trading dashboard for Interactive Brokers and Kraken.

It combines:

- Interactive Brokers account data via the async wrapper [`ib-cl-wrap`](https://github.com/reiterstephan-tech/ib-cl-wrap)
- Kraken balances, open orders, ticker prices, and derived portfolio valuation
- A WebSocket-fed ClojureScript dashboard with charts and an authenticated UI

Current dashboard features:

- IB portfolio balance, positions, and open orders
- Kraken portfolio value, balances, open orders, and ticker data
- Portfolio charts for Kraken asset distribution and Kraken vs IB breakdown
- Manual IB debug actions from the browser

## 2. Architecture

Main components:

- `trader1`: HTTP server, WebSocket broadcaster, authenticated dashboard UI
- `ib-cl-wrap`: async IB API wrapper (`core.async` + snapshot helpers)
- Kraken HTTP API: crypto balances, orders, and ticker pricing
- IB TWS / IB Gateway: broker runtime endpoint
- IB Java API (`ibapi.jar`): required Java classes used by the wrapper

```text
Browser
   |
   v
trader1 Web UI + WebSocket server
   |                     |
   v                     v
ib-cl-wrap          Kraken API
   |
   v
TWS / IB Gateway
```

## 3. Requirements

- Java 17+ (required by shadow-cljs build tooling and modern Clojure dependencies; Java 21 LTS recommended)
- Leiningen (this repository is Leiningen-based)
- Interactive Brokers TWS or IB Gateway
- IB API JAR file (`ibapi.jar`)

## 4. Interactive Brokers Setup

In TWS (or equivalent Gateway API settings):

- Enable API socket access (`Enable ActiveX and Socket Clients`)
- Allow local connections (`127.0.0.1` / localhost)

Default IB API ports:

- Paper TWS: `7497`
- Live TWS: `7496`
- Paper Gateway: `4002`
- Live Gateway: `4001`

Important:

- `client-id = 0` is required to include manual TWS orders in open-order snapshots.
- Using other client IDs generally restricts visibility to API-submitted orders.

## 5. Installation

Clone the repository:

```bash
git clone https://github.com/reiterstephan-tech/trader1.git
cd trader1
```

Initialize submodules (required):

```bash
git submodule update --init --recursive
```

IB API JAR:

- Download `ibapi.jar` separately from Interactive Brokers.
- Place it at:

```text
lib/ibapi.jar
```

Dependency details:

- `ib-cl-wrap` is included as a pinned git submodule dependency.
- Current pinned commit:
  - `bd8d10d395c3e2faa26bdbc4830e310e3fc0bdfa`

## 6. First Run Checklist

From a clean checkout, bootstrap the project in this order:

```bash
git submodule update --init --recursive
npm install
npx shadow-cljs compile app
lein test
```

Notes:

- `resources/public/js/` is generated by `shadow-cljs`. It is required at runtime but should not be edited by hand.
- If `node_modules/` is missing, the CLJS build will fail on missing packages such as `react-dom`.
- If the `vendor/ib-cl-wrap` submodule is missing, the backend and tests will fail to load `ib.*` namespaces.

## 7. Configuration

Runtime IB connection is configured via environment variables in the current implementation:

- `IB_HOST` (default: `127.0.0.1`)
- `IB_PORT` (default: `4002`)
- `client-id` is fixed to `0` in code
- snapshot timeout is fixed to `5000 ms` in code

Reference configuration map (conceptual shape):

```clojure
{:ib {:host "127.0.0.1"
      :port 4002
      :client-id 0
      :timeout-ms 5000}}
```

Field meaning:

- `:host`: IB API host
- `:port`: IB API socket port
- `:client-id`: IB client identifier (`0` required for manual TWS orders)
- `:timeout-ms`: snapshot timeout per IB request

Local runtime files:

- `credentials/kraken.key` — Kraken API credentials
  - line 1: API key
  - line 2: base64-encoded API secret
  - copy from `credentials/kraken.key.example`
- `config/auth.edn` — Web UI users and password hashes
  - copy from `config/auth.edn.example`
- `config/settings.edn` — Polling interval overrides
  - optional; copy from `config/settings.edn.example` if you want local overrides

These files are local machine configuration, are gitignored, and are required for a fully working authenticated runtime.

## 8. Running the Application

Start the server:

```bash
lein run
```

For the browser bundle, build the frontend in another terminal:

```bash
npx shadow-cljs watch app
```

Optional custom HTTP port:

```bash
PORT=8080 lein run
```

Startup behavior:

- Connects to IB (`ib.client/connect!`) with `client-id = 0`
- Subscribes to IB events
- Starts async snapshot refresh loop (default every 10 seconds)
- Starts a Kraken polling loop for ticker, balance, and open-order data
- Loads and pushes dashboard data for:
  - IB portfolio balance (`NetLiquidation`, `BuyingPower`)
  - IB positions
  - IB open orders
  - Kraken balances, open orders, ticker, and derived portfolio totals

Open:

```text
http://localhost:3000
```

## 9. Dashboard Layout

The dashboard is a ClojureScript SPA mounted into `#app` and currently includes:

- Portfolio charts at the top
- A four-card Kraken section
- A four-column IB section with balance, spacer, positions, and open orders
- An IB debug panel with manual test actions

## 10. Error Handling

Current behavior:

- IB timeout in a snapshot cell: shows `IB Timeout` in that cell only
- Lost IB session / disconnected runtime: affected cells show `Disconnected`
- Missing `ibapi.jar`: IB connection initialization fails immediately; startup logs and UI reflect IB connection error
- IB error events: logged server-side and emitted to the UI
- Kraken request failures are swallowed and retried on the next polling cycle

## 11. Development

Run tests:

```bash
lein test
```

Normal development workflow:

1. Start the frontend compiler in one terminal:

```bash
npx shadow-cljs watch app
```

2. Start the backend server in a second terminal:

```bash
lein run
```

3. Open `http://localhost:3000` and sign in.
4. Edit backend code under `src/trader1/` and restart `lein run` when needed.
5. Edit frontend code under `src/trader1/frontend/`; `shadow-cljs watch app` will rebuild automatically.
6. Run `lein test` after backend or contract changes.

Useful one-off commands:

```bash
npx shadow-cljs compile app
lein test trader1.web-test
make vendor-status
```

Notes:

- Snapshot calls use async flows (`core.async/go`) through `ib-cl-wrap`
- No blocking logic is run inside IB callback threads
- A clean checkout also needs `git submodule update --init --recursive`
- Frontend output is expected at `resources/public/js/main.js`
- Boundary contracts are lightly validated with `clojure.spec` in `src/trader1/specs.clj`
- Settings and outbound WebSocket payloads are validated before being persisted or broadcast

## 12. WebSocket Contract

Server-to-client messages are spec-checked before broadcast. Current payload shapes:

| `type` | `data` shape |
|---|---|
| `connection` | `{status: "connected"\|"disconnected"}` |
| `kraken-balance` | `{XXBT: "0.5", ZUSD: "1000.0", ...}` |
| `kraken-orders` | `{open: {txid -> order}}` |
| `kraken-ticker` | `{pairName -> {c, a, b}}` |
| `kraken-portfolio-value` | `{total-value: "12345.67", positions-value: "12000.00", cash-usd: "345.67"}` |
| `portfolio-balance` | `[{account, net-liquidation, buying-power, currency}]` |
| `positions` | `[{symbol, sec-type, currency, position, avg-cost}]` |
| `orders` | `[{symbol, action, order-type, quantity, limit-price, status, filled, remaining}]` |
| `cell-error` | `{cell: "balance"\|"positions"\|"orders", message: null\|string}` |
| `ib-error` | `{message: string}` |

## 13. Security Notice

- `client-id = 0` binds manual TWS orders into the open-order view.
- This increases operational sensitivity when connected to live accounts.
- Validate behavior against Paper accounts first, then move to Live deliberately.
